<?xml version="1.0" encoding="UTF-8"?>

<mule xmlns:http="http://www.mulesoft.org/schema/mule/http"
      xmlns="http://www.mulesoft.org/schema/mule/core"
      xmlns:doc="http://www.mulesoft.org/schema/mule/documentation"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xsi:schemaLocation="
      http://www.mulesoft.org/schema/mule/core http://www.mulesoft.org/schema/mule/core/current/mule.xsd
      http://www.mulesoft.org/schema/mule/http http://www.mulesoft.org/schema/mule/http/current/mule-http.xsd">

    
    <flow name="get-users-interface">

        <http:listener
                config-ref="external-http-listener"
                path="/users"
                allowedMethods="GET"/>

        <logger level="INFO"
                message="External API /users triggered"/>

        <flow-ref name="get-users-impl"/>

    </flow>

   
    <flow name="get-posts-interface">

        <http:listener
                config-ref="external-http-listener"
                path="/posts"
                allowedMethods="GET"/>

        <logger level="INFO"
                message="External API /posts triggered"/>

        <flow-ref name="get-posts-impl"/>

    </flow>

</mule>